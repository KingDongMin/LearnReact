let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_176‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_176‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_176‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_176‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_176‍.w("./api",[["default",["api"],function(v){api=v}]]);_176‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config(); 









const {PORT, MONGO_URI} = process.env;

mongoose
.connect(MONGO_URI)
.then(()=>{
  console.log("Connected to MongoDB");

})
.catch(e=>{
  _176‍.g.console.log(e);
});

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우터 적용

// 라우터 적용 전에 bodyParser를 적용해야 한다. 왜?
app.use(bodyParser());
app.use(jwtMiddleware); // jwt는 router 전에 할당

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, ()=>{
    _176‍.g.console.log("Listening to port %d", port);
})

