let Post;_8d3‍.x([["write",()=>write]]);_8d3‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);


/**
 * POST / api / posts
 * {
 *  title : 제목,
 *  body : 내용,
 *  tags : [ 태그1 , 태그2 ]
 * 
 * }
 */
       const write = async ctx =>{
    const { title, body, tags } = ctx.request.body;
    const post = new Post({
        title,
        body,
        tags,
    });
    try {
        await post.save();
        ctx.body = post;
    } catch (error) {
        ctx.throw(500, error)
    }
};

// export const list = ctx =>{}

// export const read = ctx =>{}

// export const remove = ctx => {}

// export const update = ctx => {}